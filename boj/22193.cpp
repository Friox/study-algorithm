#include <stdio.h>
int main() {
    int n, m;
    char a[50000] = { 0 };
    char b[50000] = { 0 };
    char result[100000] = { 0 };
    scanf("%d %d", &n, &m);
    scanf("%s", a);
    scanf("%s", b);

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            
        }
    }
    /*
    long long a, b, c, d;
    scanf("%lld %lld %lld %lld", &a, &b, &c, &d);
    printf("%lld", c * d);
    */
}